import React, { Component } from 'react'
import { Grid,Cell } from 'react-mdl'

export default class contact extends Component {
    render() {
        return (
            <div>
                <Grid>
                <Cell col={6}>first half</Cell>
                <Cell col={6}>first half</Cell>
                </Grid>
            </div>
        )
    }
}
